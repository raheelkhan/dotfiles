# Vi mode for copy/scroll (k/j to scroll, v to select, y to copy)
set-window-option -g mode-keys vi

# Mouse support (trackpad scroll enters copy mode automatically)
set -g mouse on

# Send focus events to apps (lets nvim auto-reload changed files)
set -g focus-events on
